;(function ($, Monsters, undefined){

    // set up main nav expand/collapse ////////////////////////////////////////
    $(document).ready(function() {
        //initialise the carouel
        $(".feature-carousel").owlCarousel({
            slideSpeed : 300,
            paginationSpeed : 400,
            singleItem:true
        });
    });
    
    
})(jQuery, window.Monsters || (window.Monsters = {}));